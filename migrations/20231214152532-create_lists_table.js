'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  up: async (queryInterface, Sequelize) => {
    await queryInterface.createTable('Lists', {
      id: {
        allowNull: false,
        autoIncrement: true,
        primaryKey: true,
        type: Sequelize.INTEGER,
      },
      title: {
        type: Sequelize.STRING,
        allowNull: false,
      },
      reservationEndDate: {
        type: Sequelize.DATE,
        allowNull: false,
      },
      createdAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },
      updatedAt: {
        allowNull: false,
        type: Sequelize.DATE,
      },
    });

    // Ajouter une colonne pour la clé étrangère
    await queryInterface.addColumn('Gifts', 'ListId', {
      type: Sequelize.INTEGER,
      references: {
        model: 'Lists',
        key: 'id',
      },
      onUpdate: 'CASCADE',
      onDelete: 'SET NULL',
    });
  },

  down: async (queryInterface, Sequelize) => {
    // Supprimer la colonne pour la clé étrangère
    await queryInterface.removeColumn('Gifts', 'ListId');
    
    await queryInterface.dropTable('Lists');
  },
};